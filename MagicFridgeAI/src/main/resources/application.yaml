spring:
  application:
    name: MagicFridgeAI

  datasource:
    url: jdbc:postgresql://localhost:5432/magicfridge
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

  flyway:
    enabled: true